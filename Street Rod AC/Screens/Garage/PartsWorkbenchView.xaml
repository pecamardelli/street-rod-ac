<UserControl x:Class="Street_Rod_AC.Screens.Garage.PartsWorkbenchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:Street_Rod_AC.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1280">
    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="#D0141414"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>
        <Style x:Key="PanelTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,2,12,2"/>
        </Style>
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </UserControl.Resources>

    <!-- No background: the 3D view below takes every click that does not land on a panel -->
    <Grid>
        <!-- Shelf: loose parts, pick one to see where it goes -->
        <Border Style="{StaticResource PanelStyle}"
                helpers:Reveal.IsShown="{Binding IsOpen}"
                helpers:Reveal.OffsetX="-60"
                Width="260"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Margin="20,180,0,30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="PARTS SHELF" Style="{StaticResource PanelTitleStyle}"/>

                <TextBlock Grid.Row="1"
                           Text="Nothing on the shelf. Parts you take off a car or buy end up here."
                           Style="{StaticResource LabelStyle}"
                           TextWrapping="Wrap"
                           Visibility="{Binding HasShelfItems, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Shelf}"
                         SelectedItem="{Binding SelectedShelfItem, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Visibility="{Binding HasShelfItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="row"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="1"
                                                CornerRadius="3"
                                                Padding="8,5"
                                                Margin="0,1">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="row" Property="Background" Value="#30FFFFFF"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="row" Property="Background" Value="#3059FF73"/>
                                                <Setter TargetName="row" Property="BorderBrush" Value="#59FF73"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="13"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                        <Run Text="{Binding Kind, Mode=OneWay}"/>
                                        <Run Text="  ·  "/>
                                        <Run Text="{Binding Detail, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding WorthDisplay}"
                                           FontSize="12"
                                           Foreground="#90EE90"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2"
                            Margin="0,8,0,0"
                            Visibility="{Binding ShelfHint, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="{Binding ShelfHint}"
                               FontSize="13"
                               Foreground="#59FF73"
                               TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Never mind"
                                Command="{Binding CancelMountCommand}"
                                Style="{StaticResource DialogSecondaryButtonStyle}"
                                Padding="12,4"
                                FontSize="13"/>
                        <Button Content="Take apart"
                                Command="{Binding TakeApartCommand}"
                                Visibility="{Binding CanTakeApart, Converter={StaticResource BooleanToVisibilityConverter}}"
                                ToolTip="Take off what is mounted on it, here on the shelf"
                                Style="{StaticResource DialogSecondaryButtonStyle}"
                                Padding="12,4"
                                FontSize="13"
                                Margin="8,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Engine sheet and the part that is clicked on -->
        <StackPanel helpers:Reveal.IsShown="{Binding IsOpen}"
                    helpers:Reveal.OffsetX="60"
                    Width="260"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,30,20,0">
            <Border Style="{StaticResource PanelStyle}">
                <StackPanel>
                    <TextBlock Text="ENGINE" Style="{StaticResource PanelTitleStyle}"/>
                    <TextBlock Text="{Binding EngineName}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,6"/>
                    <Grid Visibility="{Binding HasEngine, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Power" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PowerDisplay}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Torque" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TorqueDisplay}" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DisplacementDisplay}" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Gearbox" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding GearsDisplay}" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Weight" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding MassDisplay}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                    <TextBlock Text="{Binding StatusDisplay}"
                               FontSize="13"
                               Foreground="#FF8C6B"
                               TextWrapping="Wrap"
                               Margin="0,6,0,0"
                               Visibility="{Binding StatusDisplay, Converter={StaticResource StringToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource PanelStyle}"
                    Margin="0,12,0,0"
                    helpers:Reveal.IsShown="{Binding HasSelection}"
                    helpers:Reveal.OffsetY="-14">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedKind}" Style="{StaticResource PanelTitleStyle}"/>
                    <TextBlock Text="{Binding SelectedName}"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Condition" Style="{StaticResource LabelStyle}"/>
                        <ProgressBar Grid.Column="1"
                                     Minimum="0" Maximum="1"
                                     Value="{Binding SelectedCondition, Mode=OneWay}"
                                     Height="8"
                                     Foreground="#90EE90"
                                     Background="#303030"
                                     BorderThickness="0"
                                     VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding SelectedConditionDisplay}" Style="{StaticResource ValueStyle}" Margin="10,2,0,2"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Worth" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding SelectedWorthDisplay}" Style="{StaticResource ValueStyle}" Foreground="#90EE90"/>
                    </StackPanel>

                    <TextBlock Text="{Binding SelectedAttachedDisplay}"
                               Style="{StaticResource LabelStyle}"
                               TextWrapping="Wrap"
                               Visibility="{Binding SelectedAttachedDisplay, Converter={StaticResource StringToVisibilityConverter}}"/>

                    <!-- Placement mode (F5): a tool for fitting the converted parts -->
                    <Border Background="#33FFD700" BorderBrush="#FFD700" BorderThickness="1" Padding="6" Margin="0,6,0,0"
                            Visibility="{Binding PlacementDisplay, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding PlacementDisplay}" FontSize="11" FontFamily="Consolas" Foreground="#FFE8A0" TextWrapping="Wrap"/>
                    </Border>

                    <Button Content="Take it off"
                            Command="{Binding RemoveSelectedCommand}"
                            Style="{StaticResource DialogPrimaryButtonStyle}"
                            Padding="15,5"
                            FontSize="14"
                            HorizontalAlignment="Left"
                            Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Click a part to pick it. Drag to look around, scroll to get closer."
                       FontSize="12"
                       Foreground="#B0FFFFFF"
                       TextWrapping="Wrap"
                       Margin="4,10,0,0"
                       Visibility="{Binding HasSelection, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
