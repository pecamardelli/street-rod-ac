<UserControl x:Class="Street_Rod_AC.Screens.Settings.SettingsScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="800">
    <!-- The Settings card of the main screen -->
    <Border Style="{StaticResource MenuCardStyle}">
        <StackPanel>
            <TextBlock Text="SETTINGS" Style="{StaticResource MenuCardTitleStyle}"/>

            <!-- Assetto Corsa folder: read at start-up, so a change asks for a restart -->
            <Border Background="#80000000"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="15"
                    Width="560"
                    Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Assetto Corsa folder"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding AssettoCorsaFolder, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{StaticResource BgDarkBrush}"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderLightBrush}"
                                 Padding="6,4"
                                 VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1"
                                Content="Browse..."
                                Command="{Binding BrowseAssettoCorsaFolderCommand}"
                                Style="{StaticResource DialogSecondaryButtonStyle}"
                                Width="100"
                                Margin="10,0,0,0"/>
                    </Grid>
                    <TextBlock Text="{Binding AssettoCorsaFolderHint}"
                               Foreground="{StaticResource AccentPrimaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,6,0,0"
                               Visibility="{Binding AssettoCorsaFolderHint, Converter={StaticResource StringToVisibilityConverter}}"/>
                    <Button Content="Save"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource DialogPrimaryButtonStyle}"
                            Width="120"
                            HorizontalAlignment="Right"
                            Margin="0,12,0,0"/>
                </StackPanel>
            </Border>

            <!-- Edit Car Catalog Button -->
            <Button Content="Edit Car Catalog"
                    Command="{Binding OpenCatalogEditorCommand}"
                    Style="{StaticResource DialogPrimaryButtonStyle}"
                    Width="200"
                    Height="45"
                    FontSize="{StaticResource FontSizeSubheading}"
                    HorizontalAlignment="Center"/>

            <Button Content="Back"
                    Command="{Binding BackCommand}"
                    Style="{StaticResource DialogSecondaryButtonStyle}"
                    Width="150"
                    Height="45"
                    HorizontalAlignment="Center"
                    Margin="0,16,0,0"
                    FontSize="{StaticResource FontSizeSubheading}"/>
        </StackPanel>
    </Border>
</UserControl>
